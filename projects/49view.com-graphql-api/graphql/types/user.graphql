type User {
    _id: ID!
    name: String!
    email: String!
    properties: [Property]
    entities: [Entity]
}

type Query {
    users: [User]
    user(name: String!): User
    properties: [Property]
    entities: [Entity]
}
